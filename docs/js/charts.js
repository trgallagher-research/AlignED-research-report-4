/**
 * AlignED Report 4 — Charts
 *
 * The dumbbell chart was originally rendered with Chart.js (scatter plot),
 * but the y-axis labels were unreliable on a linear scale. The chart has
 * been replaced with a pure HTML/CSS implementation in results.html and
 * style.css, which provides clear, static row labels without any JavaScript.
 *
 * This file is retained as a no-op so the <script> tag in results.html
 * does not produce a 404 if not yet removed.
 */
